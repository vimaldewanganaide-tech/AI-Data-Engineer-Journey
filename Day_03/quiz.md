# Day 3 Practice Quiz

1. What is the difference between `==` and `is` in Python?
2. List three types of Python operators and give an example of each.
3. How does operator precedence affect the expression `3 + 4 * 2`?
4. What is the purpose of `elif` in a conditional statement?
5. Write a Python `for` loop that prints numbers 1 through 5.
6. When would you use a `while` loop instead of a `for` loop?
7. What does the `continue` statement do inside a loop?
8. How does `break` change loop execution?
9. Give an example of a list comprehension that squares each value in a list.
10. What does `pass` do inside a Python block?
11. In SQL, what does the `WHERE` clause do?
12. How do you select only unique values from a column in SQL?
13. What is the use of `ORDER BY` in a SQL query?
14. How does `LIMIT` change SQL query results?
15. Write a SQL condition using `LIKE` to find names that start with `A`.
16. What does `IN` do in SQL filtering?
17. When should you use `BETWEEN` instead of multiple `AND` comparisons?
18. How do you test for missing values in SQL?
19. What is the difference between `AND` and `OR` in SQL conditions?
20. Why is it often better to push filtering into the database rather than filtering in Python?
