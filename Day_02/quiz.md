# Day 2 Practice Quiz

1. What is the difference between mutable and immutable objects in Python?
2. Give two examples of immutable Python types.
3. What does `id()` return for a Python object?
4. How does `isinstance(value, type)` differ from `type(value) == type`?
5. What SQL constraint ensures every row has a unique value in a column?
6. What does `NOT NULL` mean in a table schema?
7. Why is `NULL` not the same as `0` or an empty string?
8. Which SQL clause is used to create a new table?
9. What is a primary key and why is it important?
10. How do you enforce a one-to-many relationship between tables in SQL?
11. What is the default value for `is_active` in the Student table schema?
12. In SQLite, which data type would you use for a decimal grade point average?
13. What happens when you append an item to a list?
14. Why can small integers sometimes have the same object identity in Python?
15. Describe when you would use a `dict` instead of a `list`.
